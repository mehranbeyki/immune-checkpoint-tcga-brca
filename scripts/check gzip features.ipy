import gzip

with gzip.open("/Users/mehranbeyki/portfolio-projects/scrna-immune-checkpoint-response/data/raw/gex/scRNAseq PT1/features.tsv.gz", "rt") as f:
    lines = f.readlines()

print(f" all lines : {len(lines)}\n")
print("10 lines from the end:")
for line in lines[-10:]:
    print(line.strip())